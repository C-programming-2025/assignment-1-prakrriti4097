#include <stdio.h>
int main() {
    int a = 2, b = 3, c = 4;
    int sumOfSquares = (a * a) + (b * b) + (c * c);
    printf("The sum of the squares of %d, %d, and %d is %d\n", a, b, c, sumOfSquares);
    return 0;
}